# runtime/core/src / runtime/core/src

Python source root for the core runtime.

核心运行时的 Python 源码根目录。

## Files / 文件

| Entry | Responsibility / 职责 |
|---|---|
| `runtime/core/src/cy_exec/` | Canonical inference worker package. / 标准推理工作器包。 |

## Suggested reading order / 推荐阅读顺序

Start with `runtime/core/src/cy_exec/` and then follow the module imports or package entry point.
Read sibling modules in the order required by the runtime path; consult parent and package READMEs for boundaries.
从 `runtime/core/src/cy_exec/` 开始，再按模块导入关系或包入口继续阅读。
按运行时路径阅读同级模块；边界说明请查阅父目录和包 README。
