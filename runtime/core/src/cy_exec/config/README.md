# runtime/core/src/cy_exec/config / runtime/core/src/cy_exec/config

Worker and model configuration loading and validation.

工作器与模型配置的加载和校验。

## Files / 文件

| Entry | Responsibility / 职责 |
|---|---|
| `runtime/core/src/cy_exec/config/__init__.py` | Package initializer and public import boundary. / 包初始化与公开导入边界。 |
| `runtime/core/src/cy_exec/config/config_loader.py` | Python runtime or test module: config_loader.py. / Python 运行时或测试模块：config_loader.py。 |
| `runtime/core/src/cy_exec/config/hot_reload.py` | Python runtime or test module: hot_reload.py. / Python 运行时或测试模块：hot_reload.py。 |
| `runtime/core/src/cy_exec/config/models.py` | Python runtime or test module: models.py. / Python 运行时或测试模块：models.py。 |
| `runtime/core/src/cy_exec/config/validator.py` | Python runtime or test module: validator.py. / Python 运行时或测试模块：validator.py。 |

## Suggested reading order / 推荐阅读顺序

Start with `runtime/core/src/cy_exec/config/__init__.py` and then follow the module imports or package entry point.
Read sibling modules in the order required by the runtime path; consult parent and package READMEs for boundaries.
从 `runtime/core/src/cy_exec/config/__init__.py` 开始，再按模块导入关系或包入口继续阅读。
按运行时路径阅读同级模块；边界说明请查阅父目录和包 README。
