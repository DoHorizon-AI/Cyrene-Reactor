# runtime/core/src/cy_exec/utils / runtime/core/src/cy_exec/utils

Transport authentication, TLS, tracing, path, and stream helpers.

传输认证、TLS、追踪、路径与流辅助函数。

## Files / 文件

| Entry | Responsibility / 职责 |
|---|---|
| `runtime/core/src/cy_exec/utils/__init__.py` | Package initializer and public import boundary. / 包初始化与公开导入边界。 |
| `runtime/core/src/cy_exec/utils/auth.py` | Python runtime or test module: auth.py. / Python 运行时或测试模块：auth.py。 |
| `runtime/core/src/cy_exec/utils/otel.py` | Python runtime or test module: otel.py. / Python 运行时或测试模块：otel.py。 |
| `runtime/core/src/cy_exec/utils/path_utils.py` | Python runtime or test module: path_utils.py. / Python 运行时或测试模块：path_utils.py。 |
| `runtime/core/src/cy_exec/utils/stream_buffer.py` | Python runtime or test module: stream_buffer.py. / Python 运行时或测试模块：stream_buffer.py。 |
| `runtime/core/src/cy_exec/utils/tls.py` | Python runtime or test module: tls.py. / Python 运行时或测试模块：tls.py。 |

## Suggested reading order / 推荐阅读顺序

Start with `runtime/core/src/cy_exec/utils/__init__.py` and then follow the module imports or package entry point.
Read sibling modules in the order required by the runtime path; consult parent and package READMEs for boundaries.
从 `runtime/core/src/cy_exec/utils/__init__.py` 开始，再按模块导入关系或包入口继续阅读。
按运行时路径阅读同级模块；边界说明请查阅父目录和包 README。
