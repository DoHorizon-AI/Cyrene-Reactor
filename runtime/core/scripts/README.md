# runtime/core/scripts / runtime/core/scripts

Operational and benchmark helper scripts for the core runtime.

核心运行时的运维与基准辅助脚本。

## Files / 文件

| Entry | Responsibility / 职责 |
|---|---|
| `runtime/core/scripts/benchmark.sh` | Operational helper script: benchmark.sh. / 运维辅助脚本：benchmark.sh。 |
| `runtime/core/scripts/benchmark_token_speed.py` | Python runtime or test module: benchmark_token_speed.py. / Python 运行时或测试模块：benchmark_token_speed.py。 |

## Suggested reading order / 推荐阅读顺序

Start with `runtime/core/scripts/benchmark.sh` and then follow the module imports or package entry point.
Read sibling modules in the order required by the runtime path; consult parent and package READMEs for boundaries.
从 `runtime/core/scripts/benchmark.sh` 开始，再按模块导入关系或包入口继续阅读。
按运行时路径阅读同级模块；边界说明请查阅父目录和包 README。
