# runtime/pro/src / runtime/pro/src

Python source root for Pro extensions.

Pro 扩展的 Python 源码根目录。

## Files / 文件

| Entry | Responsibility / 职责 |
|---|---|
| `runtime/pro/src/cy_exec_pro/` | Pro serving extension package. / Pro 服务扩展包。 |

## Suggested reading order / 推荐阅读顺序

Start with `runtime/pro/src/cy_exec_pro/` and then follow the module imports or package entry point.
Read sibling modules in the order required by the runtime path; consult parent and package READMEs for boundaries.
从 `runtime/pro/src/cy_exec_pro/` 开始，再按模块导入关系或包入口继续阅读。
按运行时路径阅读同级模块；边界说明请查阅父目录和包 README。
