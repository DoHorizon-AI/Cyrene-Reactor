# Placement source / 放置源代码

`main.rs` projects private host-adapter input into Platform placement. No local
allocator, Lease store, Node registry or process launcher is implemented here.

`main.rs` 将执行端事实投影到平台放置算法；不包含第二套分配器、Lease 状态库、
节点注册中心或进程启动器。
